"""Shared, domain-agnostic building blocks used by both the resume (Phase
2) and job description (Phase 3) parsers — kept here instead of one
importing from the other, since neither is conceptually "primary".
"""
